'use client';
export const hosting = {
  URL: window.location.protocol + '//' + window.location.host, // if portnumber needed
  // URL: window.location.protocol + '//' + window.location.hostname // if portnumber Not needed
};
